type ResolverType = 'Query' | 'Mutation';
type Resolvers = {
    [key in ResolverType]: {
        [key: string]: Function;
    };
};
export declare function generateResolvers(): Promise<Resolvers>;
export {};
